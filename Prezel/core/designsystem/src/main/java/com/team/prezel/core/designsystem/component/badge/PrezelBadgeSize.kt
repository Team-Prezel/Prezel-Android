package com.team.prezel.core.designsystem.component.badge

enum class PrezelBadgeSize {
    SMALL,
    REGULAR,
}

enum class PrezelBadgeActive {
    ON,
    OFF,
}

enum class PrezelBadgeDisabled {
    ON,
    OFF,
}
