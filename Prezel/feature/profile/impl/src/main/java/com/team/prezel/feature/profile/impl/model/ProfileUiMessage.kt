package com.team.prezel.feature.profile.impl.model

enum class ProfileUiMessage {
    CHECK_NICKNAME_FAILED,
    FETCH_USER_INFO_FAILED,
    LOGOUT_FAILED,
    WITHDRAW_FAILED,
    AUTHENTICATION_EXPIRED,
}
