package com.team.prezel.feature.feedback.impl.model

internal enum class FeedbackUiMessage {
    FETCH_FEEDBACK_FAILED,
    PRESENTATION_FORBIDDEN,
    PRESENTATION_NOT_FOUND,
    SELF_FEEDBACK_ALREADY_WRITTEN,
    SAVE_FAILED,
}
